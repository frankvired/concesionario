Instalar dependencias, te haces un venv y ejecutas pip install -r requirements.txt




to do:

Cuando se elimine que no se borre el coche, que se ponga en estado eliminado y se ponga la fecha de eliminacion.